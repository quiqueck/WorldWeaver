accessWidener   v1  official

extendable  class   net/minecraft/world/level/levelgen/NoiseBasedChunkGenerator

# noNewCaves/slideNetherLike widenings removed: both vanilla methods are gone/private in 26.1
# (see WoverChunkGenerator's local netherNoNewCaves()/slideNetherLike() reconstructions), so
# nothing here calls into NoiseRouterData for them anymore.
