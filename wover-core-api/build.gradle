archivesBaseName = "wover-core-api"
ext {
    baseNamespace = "core"
    baseClass = "WoverCore"
}
dependencies {
    modImplementation "net.fabricmc.fabric-api:fabric-api:${project.fabric_version}"
}

moduleDependencies(project, ['WunderLib'])